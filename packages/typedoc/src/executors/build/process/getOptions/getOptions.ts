import { ProjectConfiguration, ExecutorContext } from '@nrwl/devkit'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const getOptions = (config: ProjectConfiguration, context: ExecutorContext): Record<string, unknown> => {
  return {}
}
